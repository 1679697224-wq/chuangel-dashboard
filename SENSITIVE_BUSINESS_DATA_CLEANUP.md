# 敏感经营数据源码清理记录

本文件仅记录位置、数据类型和整改方式，不记录任何原始金额、数量或历史常量值。

| 文件位置 | 数据类型 | 整改方式 |
|---|---|---|
| `dsh_keys/master_fill.py` | 库存分类金额/数量、分仓库存分类 | 删除源码常量，改由 `CHUANGEL_BUSINESS_DATA_FILE` 指向的私有运行时JSON注入 |
| `dsh_keys/master_fill2.py` | 库存分类金额/数量、分仓库存分类 | 删除源码常量，改由同一受控运行时入口注入 |
| `dsh_keys/master_fill3.py` | 库存分类金额/数量、分仓库存分类 | 删除源码常量，改由同一受控运行时入口注入 |
| `dsh_keys/` 其他历史脚本 | 日期、换算比例及旧看板一次性替换模板 | 已完成模式扫描；新采销正式链路继续禁止导入该目录。旧看板快照模板不作为采销正式经营数据源 |

## 运行时规则

- 私有文件必须明确声明 `data_mode=RUNTIME_PRIVATE`，且不得提交Git。
- 示例或测试文件只能声明 `data_mode=MOCK`，并需显式设置 `CHUANGEL_ALLOW_MOCK_BUSINESS_DATA=1`。
- 未注入文件、文件模式不明确或必要分区缺失时，脚本立即停止，不使用内置经营数字兜底。
- 本轮没有在测试、文档、注释或提交信息中复制被清理的真实经营值。
- Git历史仍可能保留旧值；如需历史清理，须由PO另行授权并协调所有协作者。
