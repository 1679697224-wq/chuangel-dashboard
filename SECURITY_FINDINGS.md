# 安全发现（TY-AI 采销驾驶舱 V1）

本文件只记录发现与建议，不在本轮对现网代码做大规模修改；任何现存凭据均未在本文复述。

| 编号 | 等级 | 发现 | 本轮处理 | 后续建议 |
|---|---|---|---|---|
| SF-001 | 高 | 现有版本跟踪文档中存在可直接使用的登录凭据文本。 | 未读取或复制凭据值；新工程不复用该凭据。 | 立即轮换相关账号；经授权后从文档和 Git 历史清理，并检查访问日志。 |
| SF-002 | 高 | 现有登录用户数据文件被纳入 Git 跟踪；其敏感内容本轮未打开。 | 未修改登录系统；新工程使用 PBKDF2 与运行时 SQLite。 | 专项确认文件内容、加密强度、权限和历史泄露范围，迁移至安全身份存储。 |
| SF-003 | 中 | `dsh_keys/` 名称与实际包含的大量脚本/产物不一致，原忽略规则只覆盖特定私钥名称。 | 按 WorkBuddy 指定范围检查相关脚本并移除当前源码硬编码；新增常见密钥、证书及本地凭据忽略模式；未在报告复述值。 | 专项清点仓库历史，建立持续 secrets scanning 与提交前检查。 |
| SF-004 | 高 | 项目说明中的现网地址使用明文 HTTP。 | 新采销部署样例要求 TLS、反向代理和安全响应头。 | 配置 HTTPS、HSTS、证书轮换与受控域名访问。 |
| SF-005 | 中 | 现有客流爬虫正式配置被 Git 跟踪，可能包含运营参数；本轮按红线未读取。 | 文件保持原样并记录前后哈希。 | 由负责人确认配置敏感度，必要时迁移为环境变量或受控配置中心。 |
| SF-006 | 中 | 当前 GitHub 网络链路不稳定，正式推送可能受阻。 | 使用 GitHub 官方快照和 API 双重校验本地基线。 | WorkBuddy Review 前确认远端分支已成功推送且 commit 可访问。 |
| SF-007 | 严重 | WorkBuddy 确认 `dsh_keys/` 中的吉客云生产 AppKey/AppSecret 等曾硬编码并进入 Git 历史。 | 当前源码已改为从 `JKY_APP_KEY`、`JKY_APP_SECRET` 等环境变量读取；`.env.example` 仅留空占位；补充敏感文件忽略规则和轮换清单。未输出旧值。 | PO 必须撤销/轮换旧凭据、核查调用日志和仓库传播范围；代码删除不能解除历史泄露风险。历史清理由另行授权的安全任务执行。 |
| SF-008 | 高 | 旧客流爬虫数据目录曾跟踪运行期Token缓存；本轮未读取或输出其值。 | 当前Git树已移除缓存，改为环境变量或仓库外私有文件；登录、验证码、门店树和客流抓取逻辑未改变。 | PO/安全管理员确认旧Token是否仍有效并轮换，评估Git历史传播范围。 |
| SF-009 | 高 | 三份旧看板聚合脚本曾内置真实库存金额/数量常量。 | 当前源码已删除相关常量，改由显式模式的私有运行时JSON注入；未记录旧值。 | PO评估Git历史传播范围；真实运行文件必须留在仓库外受控存储。 |

## 新采销工程安全基线

- API 默认鉴权；密码使用 PBKDF2-HMAC-SHA256；会话可撤销、带过期时间。
- CORS 仅允许显式白名单，禁止 `*`。
- `.env`、运行数据库、密钥和证书不进入 Git。
- 吉客云凭据只允许从环境变量或受控密钥系统注入；测试、README、日志、注释和提交信息均不得包含真实值。
- Sandbox 与正式视图分离；未发布数据不得进入正式 KPI。
- 复核、发布和退出会话均支持审计或撤销记录。
