# 采销经营驾驶舱 V1.5 Final UI Patch 测试报告

状态：2026-09-02 自动测试、Git追溯、三档浏览器验收与Demo回归全部通过，待PO最终确认冻结V1.5 UI。

## 自动测试

- 命令：`python3 caixiao/tests/run_with_coverage.py`
- 结果：118项通过，0失败，0错误；原111项全部保留通过，本轮新增7项有业务价值的界面结构测试。
- 机器可读报告：`caixiao/coverage.xml`（Cobertura兼容）。
- 实测覆盖：645 / 1,823个可执行语句行，35.38%。
- 统计方法：Python标准库 `trace` 与AST可执行语句行；前端交互另以实际浏览器验收，不以无意义测试追逐覆盖率。

新增验证覆盖DG SI/ST真实页签切换结构、补贴五级状态、完成/时间进度参考点、DG风险列、异常可能原因、待办数据依据以及侧栏语义Token。

## `test_design_system_v15.py` Git追溯核对

- 首次出现提交：`9c8224cb0b37a2af9a213a3e7f3335e83963f34a`，该提交将文件以新增文件（`A`）纳入版本库。
- 基线文件Blob：`56768d144d1ece9e7a31c0ecabc8ca50f1a7a86c`。
- `git log --all --follow` 未发现更早提交；`git show --name-status 9c8224c...` 明确显示该文件在该提交新增。
- 本地基线与 `origin/feature/caixiao-design-system-v15` 均解析到 `9c8224c...`，远端树中存在相同Blob。
- 文件一直被Git正确跟踪；开始本轮时工作区干净，不存在本地文件与远端基线不一致，也未改写历史。
- WorkBuddy所述“未出现在9c8224c变更清单”与实际Git对象不一致；本报告仅记录可复核的Git事实。

## 浏览器实际验收

在独立本地 `DEMO_MODE=true` 服务中完成登录和实际交互，未连接真实系统。

| 验收项 | 结果 |
|---|---|
| 13个业务页面 × 1440 / 1280 / 375 | 39次路由检查全部通过 |
| 重点7页视觉检查 | 经营总览、SKU 360、库存经营、DG、单店补贴、经营异常、我的待办均通过 |
| 页面级横向溢出 | 0；宽表仅在自身容器内横向滚动 |
| 文字遮挡、卡片错位、图表裁切 | 0 |
| 筛选器断裂、菜单错位 | 0 |
| DG页签 | 点击与左右方向键均可在SI/ST之间切换，只展示当前任务 |
| 单店补贴 | 五级状态齐全，10条记录均展示完成进度和时间参考点 |
| 异常 / 待办新增字段 | “可能原因”“数据依据”均有可见列和非空内容 |
| Console Error / Warning | 0 / 0 |
| 路由与静态资源 | 19次HTTP检查均为200，404为0，资源加载失败为0，JS运行错误为0 |

## Demo模式与筛选回归

- Apple线下 → APR、Apple电商 → 京东、舒尔电商 → 天猫、分销渠道 → 分销均可正常查询并返回对应经营内容。
- 业务板块 → 渠道 → 门店/店铺三级筛选仍联动；Apple线下 → APR可加载10家门店并选择具体门店。
- 单店补贴与DG的关注/落后/严重落后分级只属于Demo前端展示规则，未写入正式经营口径；正式阈值继续待业务确认。

## 边界

本轮只修改前端展示、样式、测试和交付文档。未修改后端、API、数据库、指标口径、三级筛选关系、权限或数据治理逻辑；未连接吉客云、APR客流、钉钉或生产系统；未部署生产、未合并`main`。
