# 采销经营驾驶舱 Fix Round 1 测试报告

状态：2026-08-30 本地回归通过，待 WorkBuddy Review Round 2。

## 自动测试

- 命令：`python3 caixiao/tests/run_with_coverage.py`
- 结果：63 项通过，0 失败，0 错误。
- 机器可读报告：`caixiao/coverage.xml`（Cobertura 兼容结构）。
- 实测覆盖：494 / 1,301 个后端可执行语句行，37.97%。
- 统计方法：Python 标准库 `trace` + AST 可执行语句行；未安装第三方依赖。
- 说明：旧报告中的 92.4% 缺乏可复核产物，本轮不沿用、不推算、不美化。

## 本轮新增关键场景

- `pay_time` 与 `consign_time` 跨月订单仍进入事实层，经营主时间由发布版本读取。
- `modified_time` 增量计划及滚动回溯 + upsert 备选。
- 源订单状态聚合只给建议，人工确认字段保持未裁定。
- `INCLUDE / EXCLUDE / OFFSET / PENDING` 状态调整；取消过滤、退款/退货/红冲配置化测试。
- 销售旧/新口径订单、渠道、门店、SKU 差异；库存数量、金额、仓库、SKU、映射差异。
- 未确认事实、缺发布版本、已确认未发布版本、未知仓库均无法进入正式 KPI。
- 现货、在途、经营库存三口径及 `EXCLUDE` 仓库不计入。
- 现货WOI、含在途WOI及无库存无销量、有库存无销量、无库存有销量三个边界场景。
- 人工确认/发布审计保存 before、after、reason、affected_metrics、确认/发布时间和人员。
- API 事实写入后仍须经过映射和口径门禁。

## API 与安全检查

- 未登录正式接口：HTTP 401。
- 非白名单 Origin 预检：HTTP 403，无宽泛 CORS。
- 健康检查、业务页、维度、状态复核、同步计划、复核版本和 Sandbox 接口合同通过。
- Sandbox POST 实际返回销售差额、差异率、差异订单、渠道/门店/SKU 差异，标签为“验证数据，不代表正式经营口径”。
- 缺发布版本时正式指标 `value=null`、`status=待确认`，并返回缺失版本或未映射事实清单。
- 当前代码没有连接吉客云生产接口，没有提交新的真实经营明细或凭据。

## 浏览器检查

- 临时本地服务登录成功；采销首页、SKU 360、库存&采购、Apple政策经营四个业务页均可访问。
- 映射/口径复核页包含 `sales_adjustment_rules`、原因和受影响指标；版本表展示完整审计字段。
- 吉客云 API 复核页展示 `modified_time` 增量与禁止按发货窗口裁剪事实的规则。
- Sandbox 页面实际完成一组脱敏销售差异比较。
- 全局业务板块、品牌、渠道、日期、对比口径筛选在页面切换与刷新后继承；未接入字段明确提示，不静默伪造结果。
- SKU 搜索框可用，可进入 SKU 详情；缺数据项分别显示待接入/待确认。
- PC 1280×720：页面无加载失败；浏览器控制台 warning/error 为 0。
- 移动端 375×812：`bodyScrollWidth=375`，无横向溢出；菜单可展开；筛选器单列显示。

## 真实样本只读扫描

- `吉客云数据/销售单明细账.xlsx` 保持原样。
- SHA-256：`5168b5a7f7546e077305b8569c6e44a43f5948f4604378d8f66d335ff57c63fe`。
- 观察到 5 个不同订单状态；聚合结果见 `caixiao/docs/SALES_STATUS_REVIEW.md`。
- 未输出订单号、客户/客服备注、物流单号、唯一码或其他敏感明细。

## 受保护现网文件

本轮未修改现有总部看板、现有登录系统和客流爬虫正式配置；最终 SHA-256 将在提交前复核并记录于 `FIX_ROUND_1_REPORT.md`。
