# 采销经营驾驶舱 UI信息架构与业务前台重构测试报告

状态：2026-08-31 本地自动、API和浏览器检查通过，待PO业务体验与独立Review。

## 自动测试

- 命令：`python3 caixiao/tests/run_with_coverage.py`
- 结果：97项通过，0失败，0错误。
- 相比Framework Completion的88项，本轮新增9项UI信息架构与业务前台测试。
- 机器可读报告：`caixiao/coverage.xml`（Cobertura兼容结构）。
- 实测覆盖：643 / 1,742个可执行语句行，36.91%。
- 统计方法：Python标准库 `trace` + AST可执行语句行；不通过无价值测试追逐百分比。

## 本轮新增验证

- 主导航只有五个业务组，并包含约定的13个业务页面。
- 技术工具不出现在业务导航；系统管理入口受角色权限控制。
- 业务页面使用销售、库存、采购、政策和行动语言，不向业务用户暴露工程术语。
- Demo只显示一个低干扰提示，顶部不重复展示模式标签。
- 商品经营表有经营标签、搜索、排序、分页和SKU URL下钻。
- PC表格固定表头并支持横向滚动；375px断点可用。
- 移动端筛选默认折叠、可展开，优先展示经营判断。
- 既有raw只读、人工确认发布、KPI门禁和三模式隔离测试全部保留并通过。

## 浏览器验收

- 使用本地临时账号和纯Mock Demo Adapter完成登录；没有使用正式账号、凭据或生产数据。
- 13个业务路由全部正确加载：总览、异常、重点、商品、SKU、库存、采购、调拨、DG、补贴、政策、待办、跟踪。
- 每个业务路由的页面级横向溢出为0。
- 业务正文扫描未出现Mock、Sandbox、API、Version、Mapping、Gate、Payload、Adapter或Review Center。
- 商品“畅销”筛选返回1条实际演示记录，证明筛选不是装饰按钮。
- 375×812下页面级横向溢出为0，筛选默认收起且可以展开，移动菜单可以打开。
- 浏览器控制台warning/error为0。
- 截图保存在 `caixiao/docs/screenshots/ui-business-rebuild/`，共9张桌面端和2张移动端。

## 源码与安全检查

- Python编译、前端JavaScript语法、Git差异空白检查通过。
- `.env.example`只提供空占位和开关说明；没有提交本地登录值、密钥或真实凭据。
- 纯Mock演示数据只位于Demo Adapter，并持续标识；不含真实经营明细、客户信息或正式政策。
- FORMAL缺数据时显示待接入；SANDBOX和DEMO都不能进入正式KPI。
- 技术治理页面仍保留raw只读、版本和隔离能力，但不进入业务主导航。

## 范围说明

本轮没有连接真实吉客云、APR、IPVA/客流、钉钉或生产数据库，没有部署生产、合并`main`或开始真实联调。测试通过不等于生产验收。
