# 采销经营驾驶舱 Fix Round 2 测试报告

状态：2026-08-30 本地回归通过，待 WorkBuddy Review Round 3。

## 自动测试

- 命令：`python3 caixiao/tests/run_with_coverage.py`
- 结果：79项通过，0失败，0错误。
- 相比Round 1的63项，新墖16项。
- 机器可读报告：`caixiao/coverage.xml`（Cobertura兼容结构）。
- 实测覆盖：626 / 1,563 个可执行语句行，40.05%。
- 统计方法：Python标准库 `trace` + AST 可执行语句行；未为追求覆盖率数字安装新依赖或编写无价值测试。

## Round 2 新增场景

- `raw_name`、`raw_code` 发现后不可修改，API 修改 raw 字段被拒绝。
- `display_name` 必须经人工确认，草稿不进正式维度，发布后才可读。
- `sales/daily` 在已发布销售口径与映射下按日计算并支持日期/渠道筛选。
- `inventory/aging` 在无数据时待接入，未确认上传时待确认，确认并通过映射后输出四个库龄段。
- `anomaly/list` 在阈值未发布时不形成正式结论；发布后按配置计算可用项。
- `action/list` 只接受和返回闭集动作状态，写入经权限与审计。
- 今日、昨日、本周、本月快捷日期区间行为。
- 目标对比在缺少目标数据时显示待接入。
- SKU 参数从 URL 恢复，页面刷新不依赖 `sessionStorage`。
- 业务板块精确限定为四个枚举值，前端不存在自由输入控件。
- 三个 `master_fill` 源码不存在敏感经营总额常量，运行时数据必须显式声明模式。
- 客流 Token 缓存不在当前工作树和 Git 跟踪列表，忽略规则生效。

Round 1 的 ETL、销售状态、调整规则、Sandbox、KPI门禁、三库存、双WOI、审计和安全测试全部保留并通过。

## 源码与安全检查

- Python 编译检查通过；两个前端 JavaScript 语法检查通过。
- Git 差异空白/冲突标记检查通过。
- 三个 `master_fill` 敏感经营数字匹配为0。
- `dsh_keys` Python 脚本凭据字面量赋值匹配为0。
- 客流 Token 原文未被读取、输出或复制；当前文件已移除。
- `.env.example` 仅保留空占位。

## 浏览器检查

- 本地临时测试服务登录成功，未使用正式账号或生产数据。
- 业务板块显示四个明确选项；“本月”日期正确；“目标”应用后明确显示数据待接入。
- SKU URL 在刷新前后均恢复同一测试 SKU。
- 复核中心 raw 列可见，但无 raw 字段编辑入口。
- 375×812 下 `bodyScrollWidth=375`，无横向溢出，移动菜单可见。
- 浏览器控制台 warning/error 为0。

## 范围说明

本轮没有连接真实吉客云或客流生产系统，没有执行P2或生产部署，也没有合并 `main`。测试通过不等于生产验收。
