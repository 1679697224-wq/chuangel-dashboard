# 凭据轮换要求

状态：**需要 PO / 系统管理员立即人工处理**。

WorkBuddy 已确认历史提交中的 `dsh_keys/` 脚本曾包含吉客云生产凭据。Fix Round 1 已将当前源码中的凭据改为环境变量读取，并扩充本地敏感文件忽略规则；这只能阻止当前版本继续暴露，**不能消除 Git 历史中的泄露风险**。

## 需要轮换的凭据类型

- 吉客云 AppKey。
- 吉客云 AppSecret。
- 吉客云访问 Token / Access Token（若已签发或复用）。
- 与上述凭据绑定的其他接口会话、签名材料或衍生令牌（若存在）。

本文不记录旧值、现值或新值。

## 必须由 PO / 管理员完成

1. 在吉客云或对应开放平台撤销旧凭据并签发新凭据。
2. 核查旧凭据的权限范围，按最小必要原则重新授权。
3. 检查接口调用日志、来源 IP、时间范围和异常访问。
4. 在 GitHub 侧评估仓库可见范围、Fork、缓存、流水线日志及历史提交影响。
5. 如决定清理 Git 历史，另立安全任务，通知所有协作者重新同步；不得在本整改分支擅自重写共享历史。
6. 新凭据仅通过受控环境变量或密钥管理系统注入，不进入 Git、文档、测试、日志、注释或提交信息。
7. 完成后记录轮换时间、执行人、受影响系统和验证结果，但仍不得记录凭据值。

## 本轮未执行

- Codex 未调用或验证任何旧凭据。
- Codex 未连接吉客云生产系统。
- Codex 未执行 Git 历史重写、强制推送或仓库权限变更。
- 在 PO 完成轮换和安全核查前，不得把本项目标记为可生产部署。
