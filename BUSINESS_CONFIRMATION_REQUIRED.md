# 采销经营驾驶舱待业务确认清单

状态值：`OPEN`、`IN_REVIEW`、`CONFIRMED`、`REJECTED`。本轮全部条目保持 `OPEN`，技术上均采用配置或人工发布版本承载。

## BC-001 销售金额与退款冲销

- 问题：正式销售金额使用哪个源字段，退款、退货、取消和冲销如何计入。
- 当前发现：销售主时间已确认为 `pay_time`，金额和退款细则未确认。
- 可选方案：实付金额净额；分摊后金额净额；销售与退款分别展示后计算净额。
- 对数据的影响：销售额、毛利、客单价、目标达成率及渠道排名。
- 需要谁确认：业务负责人、财务/数据口径负责人。
- 当前状态：OPEN。

## BC-002 订单去重键与状态集合

- 问题：订单数的稳定唯一键、已付款状态集合、取消单和含退货订单如何展示。
- 当前发现：转化率分子应按 `pay_time` 订单数，但正式唯一键与状态映射未知。
- 可选方案：平台订单号；吉客云源记录 ID；主订单号与子订单号组合键。
- 对数据的影响：订单数、转化率、客单价和重复统计风险。
- 需要谁确认：业务负责人、吉客云接口负责人。
- 当前状态：OPEN。

## BC-003 三库存视图的字段规则

- 问题：实物、可销售、财务库存分别使用哪些数量、锁定量、成本和例外规则。
- 当前发现：三视图已确认，但具体源字段、公式、虚拟商品和负库存处理未确认。
- 可选方案：按吉客云标准字段；按公司自定义可销售公式；由财务提供独立库存金额口径。
- 对数据的影响：库存数量、金额、可售率、周转和补货建议。
- 需要谁确认：业务负责人、供应链负责人、财务/数据负责人。
- 当前状态：OPEN。

## BC-004 仓库、库位与门店映射

- 问题：仓库/库位的正式类型、门店归属、正品/样品/残次/在途等识别方式。
- 当前发现：任何仓库和库位首次进入正式 KPI 前必须人工确认。
- 可选方案：吉客云系统类型字段；人工维护主数据表；系统字段加例外白名单。
- 对数据的影响：库存归属、门店可销售库存、调拨和库龄。
- 需要谁确认：供应链负责人、业务负责人。
- 当前状态：OPEN。

## BC-005 渠道、门店与店铺映射

- 问题：销售渠道、法人、平台店铺、门店之间的标准层级和历史变更处理。
- 当前发现：源名称可能变化，不能直接作为长期主键。
- 可选方案：渠道主数据编码；多级映射表；按生效日期维护版本。
- 对数据的影响：渠道销售、目标达成、排名和权限范围。
- 需要谁确认：业务负责人、各渠道负责人。
- 当前状态：OPEN。

## BC-006 SKU/SPU 主数据与唯一键

- 问题：SKU、SPU、货号、条码、序列号的唯一性与商品生命周期规则。
- 当前发现：所有 SKU/SPU 必须经人工确认版本后进入正式商品视图。
- 可选方案：吉客云货品编号；公司商品主数据编码；组合键加历史映射。
- 对数据的影响：SKU 360、库存、采购、政策适用和序列化追踪。
- 需要谁确认：商品/采购负责人、吉客云接口负责人。
- 当前状态：OPEN。

## BC-007 吉客云 API 契约

- 问题：销售、库存、采购、调拨接口地址、鉴权、限流、分页、增量时间和稳定源记录 ID。
- 当前发现：当前环境没有真实 API 文档或凭据，适配器保持禁用。
- 可选方案：官方开放 API；经批准的中间服务；定时导出作为过渡且仅进入 Sandbox。
- 对数据的影响：全链路可追溯、增量同步、去重、重跑与数据新鲜度。
- 需要谁确认：吉客云接口负责人、IT/安全负责人。
- 当前状态：OPEN。

## BC-008 采购状态与预计到货

- 问题：采购单状态、未结数量、取消、部分到货、关闭和 ETA 规则。
- 当前发现：任务包要求采购全链路，但无已确认字段与状态映射。
- 可选方案：按吉客云标准状态；公司自定义业务状态；标准状态加人工例外。
- 对数据的影响：在途采购量、缺货预警、资金占用和补货动作。
- 需要谁确认：采购负责人、供应链负责人。
- 当前状态：OPEN。

## BC-009 调拨全链路规则

- 问题：调拨申请、出库、在途、入库、取消及异常的状态和数量口径。
- 当前发现：调拨 API 和字段均待实际联调。
- 可选方案：单据头状态；明细行状态；按事件流重建。
- 对数据的影响：分仓可用库存、在途库存和跨店调拨建议。
- 需要谁确认：供应链负责人、吉客云接口负责人。
- 当前状态：OPEN。

## BC-010 Apple 政策经营口径

- 问题：政策版本、有效期、适用渠道/SKU、目标、阶梯、返点和冲回规则。
- 当前发现：本轮没有可提交的正式政策数据。
- 可选方案：政策结构化台账；文件解析后人工确认；财务结算结果反向核对。
- 对数据的影响：政策达成、预计返点、利润和经营动作。
- 需要谁确认：业务负责人、Apple 业务负责人、财务负责人。
- 当前状态：OPEN。

## BC-011 目标版本与运营/财务口径

- 问题：APR 运营目标与财务目标的优先级、版本、生效期间和差异展示。
- 当前发现：目标存在，但两个口径尚未统一。
- 可选方案：并列展示；业务负责人指定主版本；分场景使用并标记来源。
- 对数据的影响：目标达成率、排名、预警和考核。
- 需要谁确认：业务负责人、财务负责人、APR 负责人。
- 当前状态：OPEN。

## BC-012 库龄分段与滞销阈值

- 问题：库龄起算时间、分段、不同品类的滞销阈值和例外。
- 当前发现：无统一正式阈值。
- 可选方案：统一分段；品类分段；政策期内动态分段。
- 对数据的影响：库龄结构、滞销金额、清货和暂停采购建议。
- 需要谁确认：采购/商品负责人、业务负责人。
- 当前状态：OPEN。

## BC-013 经营异常与动作阈值

- 问题：补货、暂停采购、调拨、分销、价格清理、活动、搭售、赠送、退货、报废、继续观察的触发与审批条件。
- 当前发现：动作词已限定，但阈值和审批链未确认。
- 可选方案：固定阈值；品类/渠道配置；规则建议加人工审批。
- 对数据的影响：异常列表、行动优先级和审计责任。
- 需要谁确认：业务负责人、对应动作负责人。
- 当前状态：OPEN。

## BC-014 业务日与跨日边界

- 问题：业务时区、日切时间和跨午夜订单归属。
- 当前发现：销售主时间为 `pay_time`，业务日边界未确认。
- 可选方案：北京时间自然日；自定义日切；按渠道分别配置。
- 对数据的影响：日销售、同比环比、目标达成和转化率。
- 需要谁确认：业务负责人、财务/数据负责人。
- 当前状态：OPEN。

## BC-015 角色、数据范围与高风险动作审批

- 问题：总部、渠道、采购、财务等角色的字段、门店/渠道范围，以及下载、导出、发布版本、AI 调用的审批。
- 当前发现：当前仅提供最小权限能力，正式角色矩阵未获确认。
- 可选方案：RBAC 加数据范围；RBAC 加临时授权有效期；接入公司统一身份与审批。
- 对数据的影响：数据泄露风险、职责分离和审计合规。
- 需要谁确认：业务负责人、IT/安全负责人、数据负责人。
- 当前状态：OPEN。

## BC-016 吉客云 modified 增量能力

- 问题：销售接口是否支持稳定按 `modified` 或等同订单更新时间分页增量，字段是否覆盖迟到更新和历史状态变化。
- 当前发现：新 ETL 已把 `modified_time` 设为首选同步游标，并提供滚动回溯 + upsert 备选；当前没有真实 API 文档和凭据可验证。
- 可选方案：稳定 modified 增量；按官方更新时间字段增量；固定回溯窗口与定期全量对账。
- 对数据的影响：跨发货月付款订单、迟到退款、状态更新、重跑去重和数据新鲜度。
- 需要谁确认：吉客云接口负责人、数据负责人、WorkBuddy 实际环境联调人。
- 当前状态：OPEN。

## BC-017 销售状态与调整动作发布

- 问题：当前及未来每个 `raw_trade_status` 应配置 `INCLUDE`、`EXCLUDE`、`OFFSET` 还是 `PENDING`，以及待审核/已付款/在途/完成如何分组。
- 当前发现：真实样本观察到发货在途、已完成、待发货-已递交、待复核、待审核；退款、退货、红冲等同名状态未在本次样本出现。
- 可选方案：按状态逐项发布；状态加业务类型联合规则；新状态默认 `PENDING`。
- 对数据的影响：销售额、订单数、退货冲减、经营状态分组及跨渠道对比。
- 需要谁确认：业务负责人、财务/数据口径负责人。
- 当前状态：OPEN。

## BC-018 OFFSET 符号与金额字段

- 问题：退款、退货、红冲的源金额是正数还是负数，`OFFSET` 应使用何种 multiplier；正式销售金额字段仍需最终确认。
- 当前发现：引擎要求 `OFFSET` 显式配置 multiplier，缺失时阻断正式 KPI；不对符号作隐式猜测。
- 可选方案：保留源负号 multiplier=1；源正数冲减 multiplier=-1；按独立退款事实关联原订单。
- 对数据的影响：净销售、退货额、订单数、跨期冲减和财务对账。
- 需要谁确认：财务负责人、业务负责人、吉客云接口负责人。
- 当前状态：OPEN。

## BC-019 全局筛选缺失字段

- 问题：业务板块、品牌的正式源字段及历史映射，对比口径的期间规则。
- 当前发现：前端已提供跨页继承的全局筛选；渠道和日期可进入查询条件，业务板块/品牌字段未进入当前事实合同，选择后必须显示待接入而不能静默忽略。
- 可选方案：扩展吉客云字段合同；从已发布 SKU/渠道主数据派生；建立独立组织/品牌主数据版本。
- 对数据的影响：首页、SKU 360、库存视图的筛选一致性和同比/环比可比性。
- 需要谁确认：业务负责人、数据负责人、商品/渠道负责人。
- 当前状态：OPEN。
