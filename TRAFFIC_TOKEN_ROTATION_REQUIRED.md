# 客流Token轮换与确认要求

状态：**需要PO或客流系统管理员人工确认**。

## 本轮发现

- Git曾跟踪客流系统访问Token缓存文件。
- 缓存同时关联客流系统用户上下文标识。
- 本轮未读取、打印或复制文件中的Token原文。

## 代码整改

- 仓库内Token缓存已移除并加入忽略规则。
- 客流客户端支持由 `IPVA_ACCESS_TOKEN`、`IPVA_USER_ID`、`IPVA_USER_NAME` 注入运行时会话。
- 也可通过 `IPVA_TOKEN_FILE` 指向仓库外私有文件；默认私有文件权限为仅当前用户可读写。
- 登录、验证码、门店树和客流抓取业务逻辑未改变。

## PO必须处理

1. 确认历史Token是否仍有效；如有效立即撤销或轮换。
2. 检查Token有效期、访问范围、关联账号和异常调用日志。
3. 评估Git历史、Fork、缓存及协作者本地副本中的传播范围。
4. 轮换完成后仅通过环境变量或仓库外私有运行时文件注入。

当前源码删除不能消除Git历史风险；本轮未重写历史或强制推送。
