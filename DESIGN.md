# 传天羽采销经营驾驶舱设计系统

版本：V1.5

适用范围：`caixiao/` 业务前台与授权管理员区域

状态：项目级唯一视觉规范

所有后续 UI 修改必须先阅读本文。本文只约束“如何呈现”，不替代业务规则、权限、数据口径、接口契约或开发治理文件。

## 1. Design Philosophy

系统服务采销经理在有限时间内判断销售、库存、政策风险与行动优先级。设计顺序固定为：先经营结论，再证据，再明细，再动作。任何装饰都不能抢占数据判断的注意力。

核心原则：可信优先于炫技；结构优先于装饰；密度服务任务；异常必须有语义；同类页面必须像同一套系统。

## 2. Visual Atmosphere

视觉定位是“成熟中国企业内部经营管理系统 + Apple 授权业务的克制专业感 + 采销高密度数据工作台”。页面应显得商务、稳定、清晰、精确，不做科技大屏、营销首页、PPT 驾驶舱、玻璃拟态或金融终端。

骨架借鉴企业设计系统的结构性；信息层级强调克制和精确；数据表强调可读、可查、可下钻；品牌感来自细节、留白与一致性，而不是营销式大字和图片。

## 3. Color Tokens

所有颜色必须使用语义 Token，不得在业务组件中继续散落无语义色值。

```css
--color-brand-primary: #194f86;
--color-brand-deep: #0b1f36;
--color-brand-light: #eaf2f9;
--color-brand-gold: #a78538;

--color-bg-page: #f4f6f8;
--color-bg-surface: #ffffff;
--color-bg-muted: #f8f9fb;
--color-bg-selected: #edf3f8;

--color-border-default: #d9e0e7;
--color-border-subtle: #e9edf1;
--color-border-strong: #bec8d3;

--color-text-primary: #172b3f;
--color-text-secondary: #52677b;
--color-text-muted: #7d8b99;
--color-text-inverse: #f7f9fb;

--color-success: #16745a;
--color-success-bg: #e8f4ef;
--color-warning: #a46213;
--color-warning-bg: #fff3df;
--color-danger: #b63838;
--color-danger-bg: #fbecec;
--color-info: #28679f;
--color-info-bg: #eaf2fa;
```

白色/灰白是内容主面；深海军蓝用于侧栏和品牌锚点；交互蓝用于链接、按钮和选中态；金色只用于品牌细节和极少量关键完成状态；红、橙、绿只表达经营状态。同一状态全系统同色。禁止渐变、彩虹图表及无业务意义的多色强调。

## 4. Typography

字体栈：`-apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif`。数字启用等宽数字特性。

| 层级 | 字号/行高 | 字重 | 用途 |
|---|---|---|---|
| 页面标题 | 20/28 | 650 | 当前业务页面 |
| 经营判断 | 18/26 | 650 | 首要结论 |
| 区块标题 | 15/22 | 650 | 页面分区 |
| 卡片标题 | 13/20 | 650 | 面板、表格标题 |
| 正文 | 13/20 | 400 | 说明和明细 |
| 表格 | 12/18 | 400 | 高密度业务数据 |
| 辅助 | 11/16 | 400 | 来源、更新时间、单位 |
| 主 KPI | 26/32 | 650 | 关键经营结果 |
| 次 KPI | 21/28 | 650 | 证据指标 |

中文不使用全大写；英文缩写保持业务标准。禁止超轻字重、巨大营销标题和用颜色替代层级。

## 5. Spacing System

基础单位 4px；常用序列为 4、8、12、16、20、24、32。页面左右 24px，区块间 20px，卡片间 12px，卡片内 16px，表格单元格横向 12px。1440 工作端保持紧凑，不用大面积空白。

## 6. Grid

内容区使用 12 列思维和 CSS Grid。侧栏 236px；页面最大宽度 1600px。常用布局为 4 列摘要、2:1 双栏、3 等栏。1280 以下可降为 3 列或单栏；375 下强制单列，表格保留横向滚动。

## 7. Sidebar

深海军蓝固定侧栏，正式白色透明 Logo 保持原比例。五个一级业务模块及顺序不得改变。一级标题强调，二级入口安静；选中态使用深浅蓝面和细金色定位线。普通用户不展示技术治理入口；管理员能力固定在侧栏底部“系统管理”。

## 8. Top Filter Bar

筛选逻辑固定为“业务板块 → 渠道 → 门店/店铺”，日期与对比是第二组。PC 使用两个带组名的紧凑区域；业务范围优先，日期快捷项在右。移动端默认折叠，展开后单列。筛选区不得占用大段首屏。

## 9. KPI Cards

KPI 分三类：

- Primary：销售额、达成率、经营库存、关键风险等，数字最突出，可有 2px 品牌顶边；
- Secondary：同比、环比、毛利、WOI、库龄等证据指标，常规尺寸；
- Status：缺货、落后、逾期等，仅以语义色左边或浅底提示。

卡片必须包含名称、值/待接入、单位和必要的目标/差额/更新时间。圆角 6px，默认无厚阴影。禁止无意义图标、渐变和全部同尺寸的卡片墙。

## 10. Tables

表格是商品、库存、采购、DG、补贴和行动页面的核心。标题与工具栏在表格上方；表头固定；名称左对齐，数量/金额/比例右对齐；行高 40–44px；支持搜索、排序、筛选、分页与点击下钻；关键首列固定；字段多时允许横向滚动。不得为了“简洁”删除业务列，也不得把表格嵌入狭窄卡片。

## 11. Charts

图表只回答明确经营问题。优先折线、柱状、堆叠柱、条形和进度条；谨慎使用环形；禁止 3D、复杂渐变和装饰性图表。每张图必须有标题、单位、时间范围、可读 Tooltip/说明、Legend（多系列时）与空状态。颜色不超过品牌蓝、深蓝、金和必要状态色。

## 12. Tabs

同层视图切换使用低高度文本 Tabs，选中态为品牌色文字和 2px 底线；不使用大胶囊。DG SI/ST 必须是两个明确的业务视图，不能混成一个指标。

## 13. Filters

页面内筛选只补充当前任务，不能复制全局经营范围。标签筛选使用紧凑分段按钮；选中态深蓝底；筛选文案必须是业务语言。任何筛选变化都需保留当前页面上下文。

## 14. Inputs

输入框高度 32–36px，边框清晰，聚焦时显示品牌蓝外圈。Label 永远可见；占位文字不承担唯一说明。只读字段使用灰底并保留可复制性。

## 15. Buttons

主按钮每个操作区最多一个；深蓝/交互蓝实底。次按钮白底灰边；危险按钮只用于真实破坏操作。按钮文案用动词，不做超大圆角；最小触控高度 36px，移动端 40px。

## 16. Badges / Status

统一经营状态：已完成/正常/领先为绿色；关注/待确认/接近为橙色；落后/严重落后/高风险/逾期为红色；一般信息为蓝色。Badge 使用浅底深字、5px 圆角，不使用彩色实心胶囊堆叠。

## 17. Empty State

正式数据为空统一显示“待接入”，并以一句话说明接入/确认条件；不生成示例数字。空状态高度随容器而定，不用插画和大面积留白。Demo 数据由全局唯一“演示数据”标识说明，业务模块不重复技术文案。

## 18. Loading State

加载中使用与最终结构近似的浅灰 Skeleton：标题条、KPI 数字条、表格行。避免整页转圈和跳动。加载文字使用“正在加载经营数据”。

## 19. Error State

错误应保留页面框架，在内容区展示“加载失败”、可理解原因和重试入口。不得暴露堆栈、接口地址、Payload 或凭据。风险红色只用于边线和标题，不做整屏红底。

## 20. Drawer / Modal

抽屉用于明细和辅助编辑，宽 480–640px；Modal 仅用于需要明确确认的短流程。标题、影响范围、主次操作固定；危险操作必须二次确认。移动端抽屉占满屏。

## 21. SKU Detail

SKU 360 顺序固定为：商品概览 → 经营判断 → 销售趋势 → 库存与 WOI → 库龄 → 渠道/仓库/门店 → 价格与利润 → 政策 → 风险与建议动作。首屏先回答当前状态，再给 7/14/28/90 天销量、三库存、双 WOI 等证据。SKU 必须可由 URL 恢复。

## 22. Responsive

核心断点：1440 目标工作端；1280 紧凑工作端；760 进入移动导航；390 专门校验 375 设备。响应式只改变排列和呈现密度，不删除业务能力。

## 23. Mobile

375px 优先展示经营总览、异常、今日重点、SKU 摘要、DG、补贴和待办。侧栏为抽屉；筛选默认折叠；KPI 单列或双列；复杂表格横向滚动并固定首列；页面级不得出现异常横向滚动。

## 24. Demo Mode

`DEMO_MODE=true` 时全局只保留一个低干扰“演示数据”标识。业务正文不得反复出现 Demo、Mock、Sandbox、API 等技术词。演示数据只验证页面、交互和流程，不能冒充真实经营事实；Formal 与 Sandbox 继续严格隔离。

## 25. Admin Area

管理员区域延续同一 Token，但用中性深灰蓝区分任务属性。技术字段、版本、映射、连接器和验证结果仅在授权管理员区域出现；普通业务导航和业务页面禁止出现技术语言。管理员表格强调只读字段、版本、确认和审计状态。

## 26. Do's

- 先呈现判断，再呈现证据和动作；
- 使用统一 Token、间距、卡片、表格和状态语义；
- 明确单位、时间范围、来源状态和更新时间；
- 保留经营字段并支持搜索、排序、分页和下钻；
- 让销售、库存、政策、行动在首页形成清楚的四象限；
- 保持白/灰内容面、深蓝导航、克制金色；
- 每次改动在 1440、1280、375 三档验证。

## 27. Don'ts

- 不改变五组菜单、三级筛选、指标名称、口径、权限和数据闸门；
- 不做 KPI 卡片墙、科技大屏、PPT 式布局或营销 Hero；
- 不使用渐变、玻璃拟态、厚阴影、巨大圆角、彩虹图表；
- 不在业务页面暴露 Demo/Mock/Sandbox/API/Mapping/Gate 等工程语言；
- 不生成销售、库存、价格、利润、政策、DG、补贴等企业事实；
- 不为了视觉简洁删除表格列或关键经营证据；
- 不在未发布映射/口径时输出正式 KPI。

## Agent Prompt Guide

后续设计任务的默认提示：先读取本文件；确认改动属于业务前台还是管理员区域；复用语义 Token 与现有 rendering helper；按“结论—证据—明细—动作”组织；不改变业务结构和数据逻辑；完成 1440/1280/375 验证，并检查业务页技术词、控制台和资源状态。
